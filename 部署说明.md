# 航空电子元件库存管理系统｜快速部署

这份说明面向第一次拿到项目的同学。推荐直接打包成 WAR 部署到 Tomcat，不需要配置 IntelliJ IDEA 的 Tomcat 插件。

## 一、准备环境

请先安装并启动以下软件：

- JDK 17
- Maven 3.8 或更高版本
- MySQL 8.0
- Tomcat 8.5

在 PowerShell 中检查 Java 和 Maven：

```powershell
java -version
mvn -version
```

两条命令都能正常显示版本后，再继续下面的步骤。

## 二、最快部署流程

### 第 1 步：初始化数据库

确认 MySQL 服务已经启动，然后使用 MySQL Workbench、Navicat 或 IDEA 的数据库工具执行：

```text
sql/init.sql
```

脚本会自动创建 `aviation_inventory` 数据库及项目需要的表。

> **重要：** `init.sql` 会删除并重新创建现有业务表。它只适合首次部署或确认可以重置数据的环境；已有业务数据时不要重复执行。

首次部署只需要执行 `init.sql`，不需要另外执行 `suppliers.sql` 或 `trend_data.sql`。

### 第 2 步：填写本机数据库账号

打开：

```text
src/main/java/com/aviation/utils/JDBCUtils.java
```

将其中的 `USER` 和 `PASSWORD` 修改为运行者自己的 MySQL 用户名和密码。如果 MySQL 使用的不是默认 `3306` 端口，再同步修改 `URL`。

不要把真实数据库密码上传到公开仓库或发到群聊中。

### 第 3 步：打包项目

在本文件所在的项目目录打开 PowerShell，执行：

```powershell
mvn clean package
```

第一次构建时 Maven 会下载依赖，需要保持网络连接。看到 `BUILD SUCCESS` 后，应生成：

```text
target/aviation.war
```

### 第 4 步：部署并启动 Tomcat

1. 把 `target/aviation.war` 复制到 Tomcat 的 `webapps` 目录。
2. 双击 Tomcat 的 `bin/startup.bat`。
3. 等待几秒，让 Tomcat 自动解压并启动项目。

目录示例：

```text
apache-tomcat-8.5/
├── bin/
│   └── startup.bat
└── webapps/
    └── aviation.war
```

### 第 5 步：打开系统

浏览器访问：

```text
http://localhost:8080/aviation/
```

进入登录页后，先注册一个账号，再使用该账号登录。

停止项目时，双击 Tomcat 的 `bin/shutdown.bat`。

## 三、部署完成后的快速检查

登录后依次确认：

- 仪表盘可以显示统计数据。
- 元件管理可以新增、修改和查询。
- 入库、出库后库存和仪表盘记录会更新。
- 分类管理和供应商管理页面可以正常打开。

## 四、更新代码后重新部署

代码修改后：

1. 先运行 Tomcat 的 `bin/shutdown.bat`。
2. 再次执行 `mvn clean package`。
3. 用新的 `target/aviation.war` 替换 Tomcat `webapps` 中的旧文件。
4. 删除 Tomcat 自动生成的旧 `webapps/aviation` 文件夹。
5. 重新运行 `bin/startup.bat`，并在浏览器中按 `Ctrl+F5` 强制刷新。

只更新程序代码时，不需要重新执行 `init.sql`。

## 五、常见问题

| 现象 | 优先检查 |
|---|---|
| `mvn` 命令不存在 | Maven 是否安装，以及 Maven 的 `bin` 是否加入了 `PATH` |
| Maven 提示 Java 版本不对 | `java -version` 和 `mvn -version` 是否都指向 JDK 17 |
| 数据库连接失败 | MySQL 服务、数据库账号密码、端口，以及是否执行了 `init.sql` |
| 浏览器显示 404 | WAR 是否位于 Tomcat 的 `webapps`，访问地址是否为 `/aviation/` |
| 8080 端口被占用 | 是否已有另一个 Tomcat 或 IDEA 中的服务器正在运行 |
| 修改后页面没有变化 | 是否重新打包并替换 WAR；尝试 `Ctrl+F5` 强制刷新 |
| Tomcat 一闪而退 | 在命令行运行 `bin/catalina.bat run` 查看具体报错 |

查看 8080 端口占用情况：

```powershell
netstat -ano | findstr :8080
```

## 六、关键文件位置

```text
project/
├── pom.xml                                      # Maven 构建配置
├── sql/init.sql                                 # 首次初始化数据库
├── src/main/java/com/aviation/utils/
│   └── JDBCUtils.java                           # 本机数据库连接配置
└── target/aviation.war                          # Maven 构建后的部署文件
```

## 七、可选：使用 IntelliJ IDEA 运行

也可以在 IDEA 中导入 `pom.xml` 后配置 Tomcat 8.5，但不同版本的 IDEA 配置方式不同，访问路径也可能显示为 `/project_war`。为了让其他人最快跑起来，仍建议优先使用上面的 WAR 部署方式；WAR 文件名固定为 `aviation.war`，访问路径就是 `/aviation/`。
